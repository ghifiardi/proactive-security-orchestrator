# Proactive Security Orchestrator Dependencies

# Core
typer>=0.9.0
rich==13.5.2
pydantic>=2.0.0
jsonschema>=4.0.0
structlog>=23.2.0

# Testing
pytest>=7.4.0
pytest-cov>=4.1.0
pytest-asyncio>=0.21.0

# Development
black>=23.0.0
flake8>=6.0.0
mypy>=1.0.0

# Note: semgrep and gitleaks should be installed separately:
# - semgrep: pip install semgrep OR brew install semgrep
# - gitleaks: brew install gitleaks OR download from https://github.com/gitleaks/gitleaks

